:root {
    {{ range $key, $font := .Fonts -}}
    --{{$key}}-font-family: "{{$font.Family}}", sans-serif;
    {{ end -}}
    {{ range $key, $color := .Colors -}}
    --{{$key}}-color: {{$color}};
    {{ end -}}
}
{{.Styles}}
