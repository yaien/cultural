name: Deploy
on: 
  push:
   branches: main
jobs:
  build:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: Build
        run: GOOS=linux GOARCH=amd64 go build ./cmd/cultural
      
